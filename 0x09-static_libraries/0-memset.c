#include <stdio.h>
#include "main.h"

/**
 * _memset - Entry point
 * @s: Given argument
 * @b: Second argument
 * @n: Third argument
 * Return: 0
*/
char *_memset(char *s, char b, unsigned int n)
{
        return (0);
}
